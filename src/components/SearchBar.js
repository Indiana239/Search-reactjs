function SearchBar({ onSubmit }) {
  const handleClick = () => {};

  return (
    <div>
      <input />
      <button onClick={handleClick}>Click</button>
    </div>
  );
}

export default SearchBar;
